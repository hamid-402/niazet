# قواعد اجرای مدار

پیش از هر تغییر، `docs/ORBIT_IMPLEMENTATION_ROADMAP.md` را کامل بخوانید. این قراردادِ تأییدشدهٔ مالک دربارهٔ حفظ قابلیت‌ها، داده‌ها، نقش‌ها و آزمون‌هاست؛ کاهش دامنه برای ساده‌کردن UI مجاز نیست.

- تنها مرجع همین پروژه است؛ نسخهٔ موازی یا پوشهٔ پشتیبان نسازید.
- برای تغییر Web، دستورالعمل `apps/web/AGENTS.md` نیز لازم است.
- `node scripts/orbit-preservation-contract.mjs` و قراردادهای مرتبط را اجرا کنید. محافظ ساختاری جای آزمون رفتاری را نمی‌گیرد.
- پس از هر مرحله، وضعیت و شواهد واقعی را در نقشه‌راه مدار ثبت کنید؛ موارد باز نقشه‌راه محصول را حفظ کنید.
