# Approved brand asset

`niazet-approved.png` is the owner's supplied `نهایی 1.png`, copied without pixel changes (1536 × 1024; 1,088,884 bytes).

SHA-256: `26365ddd831ddee75f7c5501729f4fb00055a39d3e9045e3d982b8f2f701ea92`.

`BrandMark` displays the Persian or English row with a CSS viewport. It does not redraw, recolor, remove the original background, or distort either wordmark. The light plate is intentional in both themes. Next Image serves optimized sizes; width/aspect ratio reserve space before loading. The shared original is the only source asset, not a project backup.

Consumers: public header, workspace sidebar/mobile header/drawer, and home footer. Accessible names come from image alt text; the existing tagline is retained where space permits. A future transparent/vector source must be explicitly supplied or approved by the owner, not silently substituted.
